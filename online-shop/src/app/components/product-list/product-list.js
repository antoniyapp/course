import React from 'react';

import ProductListItem from './product-list-item.js';

const ProductList = (items) => (
<div>
<ul>
{/* {items.map((item) => (<li id = {item.id}><ProductListItem item/></li>))} */}
</ul>
</div>
);

export default ProductList; 